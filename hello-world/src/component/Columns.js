import React from 'react'

function Columns() {
    return (
        /* Instead of React.Fragment tag we can use empty tag but we can't able to pass any key value inside an Empty tag */
        <> 
            <td>Name</td>            
            <td>Pradeep</td>
        </>
    )
}

export default Columns
